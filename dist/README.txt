This folder is populated automatically when you build the project in Release:

  dotnet build VolaBubbles\VolaBubbles\VolaBubbles.csproj -c Release

Commit the updated files here when you publish a new version to GitHub.
End users: see the main README.md -> Install (copy destination for Quantower).
